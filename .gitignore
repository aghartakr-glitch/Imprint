# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 로컬 컴파일 서버 산출물 (용량 문제로 미커밋)
Imprint-Data/
# fonts/는 커밋함 — Railway Docker 빌드가 GitHub 저장소만 보고 이미지에
# COPY하므로, gitignore로 빼두면 배포 빌드에서 "not found"로 실패한다.

# 환경변수 (Supabase 키 등 민감정보)
.env
